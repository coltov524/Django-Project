# Django-Project
Web application built with Django that implements CRUD functionality for data management and email notifications via SMTP. The project demonstrates the use of models, views, and templates, as well as structuring a web application using Django’s MVC architecture.
